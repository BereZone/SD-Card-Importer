# Xcode
.DS_Store
*.xcodeproj/xcuserdata/
*.xcuserdatad/
DerivedData/
build/

# Swift Package Manager
.build/

# Carthage
Carthage/Build/

# CocoaPods
Pods/

# Optional: Screenshots (if not managed by Fastlane or similar)
*.png
*.jpg

# ...but the app icon assets are build inputs, not screenshots. Without these
# a CI checkout builds an app with no icon at all.
!SD Card File Importer/Assets.xcassets/**/*.png
!sd_card.png

# Optional: API Keys / Sensitive Data (if stored in config files)
*.xcconfig

# Impeccable design-tool working state (critique snapshots, local config).
.impeccable/
